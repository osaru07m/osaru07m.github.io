import { Cascadia_Mono, Space_Mono } from 'next/font/google';

export const cascadiaMono = Cascadia_Mono({
    subsets: ['latin'],
});

export const spaceMono = Space_Mono({
    weight: ["400", "700"],
    subsets: ['latin'],
});
